{
    "$id": "",
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "schema_version": "",

    "title": "subject",
    "description": "subject metadata for an IC Electrophysiology session during a Patch-seq experiment",
    "type": "object",
    "required": ["sampleID", "sampleType", "species", "speciesNCBITaxonID"],
    "properties": {
        "sampleID": {
            "$ref": "#/$defs/sampleID"
        },
        "sampleType": {
            "$ref": "#/$defs/sampleType"
        },
        "Species": {
            "$ref": "#/$defs/species"
        },
        "speciesNCBITaxonID": {
            "$ref": "#/$defs/speciesNCBITaxonID"
        },
        "specimenID": {"$comment": "does specimenID include genotypee, creLine, reporterStatus?",
        "$ref": "#/$defs/specimenID"
        },
        "parentSpecimenType": {
            "$ref": "#/$defs/parentSpecimenType"
        },
        "subjectID": {
            "$ref": "#/$defs/subjectID"
        },
        "subjectAge": {
            "$ref": "#/$defs/subjectAge"
        },
        "subjectSex": {
            "$ref": "#/$defs/subjectSex"
        },
        "subjectDateOfBirth": {
            "$ref": "#/$defs/subjectDateOfBirth"
        },
        "genotype": {
            "$ref": "#/$defs/genotype"
        },
        "creLine": {
            "$ref": "#/$defs/creLine"
        },
        "reporterStatus": {
            "$ref": "#/$defs/reporterStatus"
        }
    },
    "additionalProperties": "",

    "$defs": {
        "sampleID": {
            "type": "string",
            "description": "The unique identifier of the biological sample"
        },
        "sampleType": {
            "type": "string",
            "description": "The specimen type of the biological sample. See Specimen Types in the Controlled Vocabulary."
        },
        "species": {
            "enum": ["mouse", "human"],
            "description": "Common english name for the species of the sample."
        },
        "speciesNCBITaxonID": {
            "type": "string",
            "description": "The unique identifier for the species of the sample."
        },
        "parentSpecimenType": {
            "type": "string",
            "description": "The type of the parent specimen associated with this sample. See Specimen Types in the Controlled Vocabulary."
        },
        "subjectID": {
            "type": "string",
            "description": "The unique identifier of the individual subject (formerly donor) providing sample."
        },
        "subjectAge": {
            "type": "string",
            "description": "Age of the donor subject when the sample was taken. Add a value and units, (e.g. '8w'; 'P56')"
        },
        "subjectSex": {
            "type": "string",
            "description": "Sex of the subject"
        },
        "subjectDateOfBirth": {
            "type": "string",
            "format": "date",
            "description": "Date of birth of subject. Can be supplied instead of 'age'."
        },
        "specimenID": {
            "type": "string",
            "description": "The unique identifier of the parent specimen associated with this sample."
        },
        "genotype": {
            "type": "string",
            "description": "genotypee of the animal as 'allele1/allele2;allele3/allele4'."
        },
        "creLine": {
            "type": "string",
            "description": "Reporter gene; modified part of the mouse genome to target a certain cell type."
        },
        "reporterStatus": {
            "type": "string",
            "description": "Cre+ (positive =  fluorescent label) or Cre- (negative = no flourescent label)."
        }
    }
}
