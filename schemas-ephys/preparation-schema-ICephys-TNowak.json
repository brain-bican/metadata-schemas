{
    "$id": "",
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "schema_version": "",

    "title": "preparation",
    "description": "preparation metadata for an IC Electrophysiology session during a Patch-seq experiment",
    "type": "object",
    "required": [],
    "properties": {
        "externalSolutionRecipe": {
            "$ref": "#/$defs/externalSolutionRecipe"
        },
        "bathTemperature": {
            "$ref": "#/$defs/bathTemperature"
        },
        "electrodeZeroedEnabled": {
            "$ref": "#/$defs/electrodeZeroedEnabled"
        },
        "restingMembranePotential": {
            "$ref": "#/$defs/restingMembranePotential"
        },
        "sealResistance_PassFail": {
            "$ref": "#/$defs/sealResistance_PassFail"
        },
        "initialAccessResistance": {
            "$ref": "#/$defs/initialAccessResistance"
        },
        "inputResistance": {
            "$ref": "#/$defs/inputResistance"
        }
    },
    "additionalProperties": "",
    "$defs": {
        "externalSolutionRecipe": ["ACSF .I", "ACSF .III", "ACSF .IV", "ACSF .VII", "ACSF .VIII"],
        "bathTemperature": {
            "type": "number",
            "description": "Temperature, in Celsius, of the ACSF solution the slice is held in during the experiment."
        },
        "electrodeZeroedEnabled": {
            "type": "boolean",
            "description": "Was the effect of electrode bias current zeroed-out? Yes/No."
        },
        "restingMembranePotential": {
            "type": "number",
            "description": "Upon break-in and formation of a stable seal (typically within the first minute and not more than 3 minutes after break-in), the resting membrane potential of the neuron is recorded."
        },
        "sealResistance_PassFail": {
            "type": "boolean",
            "description": "A gigohm seal must have been reached prior to break-in."
        },
        "initialAccessResistance": {
            "type": "number",
            "description": "The initial access resistance must be < 20 MΩ and < 15% of the Rinput (mouse) or 20% of the Rinput (human)."

        },
        "inputResistance": {
            "type": "number",
            "description": "After gigaseal formation, the input resistance of the cell is the ratio of target transient capacitance to responding current."
        }
    }
}
